name: workflow Test

on:
  push:
    branches: 
      - main
env:
  MY_NEW_VARIABLE: 'hello-\\n world'
  
jobs:
  example-job:
    runs-on: ubuntu-latest
    steps:
      - name: Print a working-directory
        run: echo "$MY_NEW_VARIABLE"
        
      # - name: checkout repository
      #   uses: actions/checkout@v6.0.2

      # - name: list repository
      #   run: ls -la
      

        
        
    
