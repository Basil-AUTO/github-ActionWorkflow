name: workflow Test

on:
  push:
    branches: 
      - main
env:
  MY_NEW_VARIABLE: >
      basil is very good
      but he is not conforutable 
  
jobs:
  example-job:
    runs-on: ubuntu-latest
    steps:
      - name: Print a working-directory
        run: echo "$MY_NEW_VARIABLE"
        
      - name: checkout repository
        uses: actions/checkout@v6.0.2

      - name: list repository
        run: ls -la
      

        
        
    
