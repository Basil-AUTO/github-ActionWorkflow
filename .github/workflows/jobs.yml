name: make any related

on: 
  workflow_dispatch: 

jobs:
  job_1:
    runs-on: ubuntu-latest
    steps:
      - name: job nubmer 1
      - run: "echo job ${{github_job}}"
  job_2:
    runs-on: ubuntu-latest
    needs: job_1
    steps:
      - name: job nubner 2
      - run: "echo job ${{github_job}}"

  job_3:
    runs-on: ubuntu-latest
    needs: job_1
    steps:
      - name: job nubmer 3
      - run: "echo job ${{github_job}}"
  job_4:
    runs-on: ubuntu-latest
    needs: job_3
    steps:
      - name: job nubner 4
      - run: "echo job ${{github_job}}"

  
